# dependencies
node_modules/

# secrets / local env
.env
.env.local
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
credentials*.json
service-account*.json

# 손 시뮬 연습장 (버려도 되는 테스트 볼트 — 배포물에 포함 금지)
sandbox-vault/

# local-only development checkpoint (never publish)
docs/original/CHECKPOINT.original.md

# 로컬 전용 검증 스크립트 (개인 볼트 경로 포함 — 공개 저장소에 올리지 않음)
plugins/wikimate/scripts/test-real-vault.mjs

# OS / editor
Thumbs.db
.DS_Store

# logs
*.log
npm-debug.log*

# 로컬 AI 도구 산출물 (공개 repo에 커밋 금지 — 이 프로젝트 코드 아님)
.omc/
.plugin-config/
.complexity-log.md
.pair-programming-session.md
.todos-report.md/
.failure-tracker.jsonl

# PDF 빌드 산출물(임시 — 커밋 안 함)
.pdfbuild/
